# Digital Thread POC — Hydrow II-AP Packer

Run:
```
pip install -r requirements.txt
python3 tests/test_end_to_end.py       # smoke test — proves the full pipeline works
streamlit run app/streamlit_app.py     # interactive demo
```

See BUILD_STATUS.md for exactly what's built, validated, and still pending.
